* IndirectMap: an indirect map, whereby the first layer of keys are merely
* placeholder keys.  Each key should have a value which is a object.
* The "real" keys of the IndirectMap are then the subkeys of the placeholder
* objects.
